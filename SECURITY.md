# 安全说明

[English](SECURITY.en.md)

## 永不公开

- 机器人秘密、令牌、绑定码或接收者编号；
- 真实提示词、回复或 rollout 文件；
- 私人渠道地址和完整本机用户路径；
- Codex OAuth／API 凭据；
- 含认证头、签名网址或个人身份信息的日志。

## 报告问题

请通过 GitHub 私密安全报告提交安全问题。不要在公开问题中粘贴秘密或真实任务内容；先提供受影响版本、最小合成复现和脱敏后的字段结构。

若通讯凭据或接收者编号已经暴露：先撤销或轮换凭据，再按平台能力解除旧绑定；清理公开内容与 Git 历史，检查本机和渠道日志，最后只公开脱敏时间线。

## 设计边界

- 通讯平台秘密只保存在受限的本机目录或平台自己的安全存储中。
- 钩子只写最小本地事件，不直接访问网络。
- 默认通知不包含提示词、工具参数、结果或完整回复。
- 入站模式由用户选择；启用时必须限制发送者与动作，并具备确认、防重放、审计、限速和关闭开关，不能把任意文字直接变成本机命令。
- 发送队列与日志必须经过脱敏，并设置保留期限。
- 真实钩子和平台绑定要由用户审查并确认。

公开测试夹具必须从零构造，不能把真实对话“改几个名字”后提交。
