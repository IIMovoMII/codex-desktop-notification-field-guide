---
name: codex-desktop-notification-field-guide
description: 用于按当前电脑环境设计、审查或修复 Codex Desktop 提醒程序；它组合生命周期、增量事件和进程状态识别用户选择的任务状态，并通过 CC Connect 发送可靠、可裁剪的通知。
---

# Codex Desktop 消息提醒经验指南

[English](SKILL.en.md)

为当前电脑构建版本相关的本地提醒程序。CC Connect 是固定消息桥，但平台、提醒范围和入站能力由用户选择；先盘点，后实施。

## 适用场景

用户希望从任何正常入口打开 Codex，在任务完成、报错、被中断或需要操作时收到提醒，同时不把安静的长任务误判成卡死。

不要在用户未选择的情况下把提醒程序扩展成绩效监控或远程控制。

## 说明书定位与实现自由度

本仓库是给智能体参考的产品说明书和经验包，不是可直接安装的成熟程序，也不规定唯一架构。结合可读取的本机系统、Codex 版本和已有组件选择实现；钩子、文件事件、进程观察、托盘程序、系统服务等都只是可选手段。用户可以增加、删除或替换状态、界面、平台和入站功能。

Windows 路径和机制是已验证案例，其他系统使用等价能力。不要询问能够从本机安全读取的事实；只询问无法推断、而且会改变成品的用户选择。

## 一句话部署的授权边界

README 中的部署提示允许只读盘点和编写本机方案，不等于允许安装 CC Connect、绑定通讯账号、写入真实钩子或启用入站控制。到达这些步骤时，说明影响并再次征得用户同意。凭据由用户在本地界面输入。

## 工作流

1. 盘点系统与 Codex Desktop 版本、进程树、钩子来源、JSONL 结构、来源标记、存储位置和 CC Connect 状态；不得输出正文或秘密。
2. 集中询问用户选择：哪些状态要提醒、消息内容范围、通讯平台、CC Connect 专用或共享、应用启动即联动还是首个任务时联动，以及是否需要入站交互。已能读取的环境事实不要再问。
3. 根据已安装 CC Connect 版本说明真实可选平台；明确不推荐个人微信承担无人值守提醒，并把本机观察、上游问题和后来修正过的结论分开说明。
4. 与用户确认最小功能范围；后续模块可组合，不要求所有实现都支持全部状态、平台或界面。
5. 让观察程序与启动入口解耦。可优先用全局 `SessionStart` 等生命周期钩子按需唤醒；需要应用进程出现即联动时，使用当前系统的进程事件或等价机制，不要求包装启动命令。
6. 写钩子前检查来源与信任状态。一个配置层只选 `hooks.json` 或内联 `[hooks]`，不要两者并存；定义改变后提醒用户重新审查。
7. 钩子只写最小本地事件并快速返回。多个同事件命令钩子会并发运行，别的 `Stop` 钩子仍可能要求续跑；因此 `Stop` 永远只能作为收尾候选，不能单独宣布完成，`stop_hook_active` 也不能证明其他并发钩子已经结束。`SessionEnd` 只用于本地会话清理，不能当每轮完成事件，也不能依赖异步执行。
8. 把当前版本已验证的结构化追加事件作为主要状态证据，把 Desktop 进程状态作为上下文。没有经过版本验证的字段标成未知。
9. 在进入状态机前过滤 CLI、子代理、观察程序自身和不在范围内的 ChatGPT 会话。
10. 为每个任务与轮次维护独立状态；只实现用户选择的状态，但每个已实现状态都要有证据、冲突优先级、恢复动作和验收条件。
11. 区分任务内部暂时重试和最终失败。错误恢复后成功只按完成处理；重试耗尽、明确终止或自动暂停才发相应终态。
12. `Stop` 后出现任何新活动都撤销其收尾候选。只用独立终态记录决定完成；若观察结束仍无终态，落到“需要检查”，不能靠固定延时猜完成。
13. Desktop 在活动任务中退出且没有维护标记时进入有界收尾；如果仍找不到终止证据，就发“结果未知，需要检查”。进程及时恢复时取消候选。
14. 每个会话只保留当前未完成轮次，旧轮次静默退役。最终通知使用跨重启稳定的事件键，渲染文字变化不能生成第二条；状态和队列提交顺序应可恢复。
15. 等待批准和等待输入依赖明确请求编号或当前版本验证过的等价信号，每个请求只提醒一次；恢复运行后清除等待状态。
16. 安静时长绝不能产生“卡死”或失败通知。观察程序自身可以有健康检查，但不能拿健康计时器给任务下结论。
17. 使用当前系统的文件变化事件唤醒增量读取器，为每个文件持久保存字节游标；处理半行、文件替换、截断和通知溢出。
18. 账号切换器或更新器使用有期限的维护标记。标记只抑制计划内进程退出，不得屏蔽明确错误。
19. 在本地完成分类、裁剪与脱敏；具体通知内容按用户选择，默认建议最小化。
20. 通知先进入持久发送队列，再交给 CC Connect；成功返回后才标记已发送。临时失败退避重试，永久失败进入死信并显示本地健康警告。
21. 区分专用与共享 CC Connect：专用进程可随观察程序启停，共享实例绝不能因为 Codex 退出而被结束。
22. 入站模式由用户选择。启用时把允许的发送者、动作、确认、审计、防重放和限速作为独立控制边界；未启用时可以像纯提醒方案一样忽略普通消息。
23. 把钩子未信任、解析器版本不兼容、监听溢出未恢复、CC Connect 未认证／额度耗尽和死信积压设为可见的观察系统故障。
24. 用合成事件测试全部竞争顺序、重启、平台故障和隐私，再通过用户选定平台发送一条真实测试通知。

## 不可违反的规则

- 不显示通讯凭据、账号标识或对话正文。
- 不在用户选择前安装或绑定通讯平台。
- 不把个人微信推荐为默认无人值守路线。
- 不从安静时长推断任务失败。
- 不在钩子中等待网络发送。
- 不把 `SessionEnd` 当作每轮完成钩子。
- 不在缺少明确证据时声称任务正在等待输入。
- 不在成功投递前删除队列项目。
- 未经用户选择和独立安全设计，不把入站聊天直接映射为本机命令。
- 不把一个 Codex 版本的字段直接套用到另一个版本。

## 按需阅读

- 从[信号检查](references/signal-discovery.md)开始。
- 用[状态机](references/state-machine.md)闭合状态竞争。
- 用[增量监听](references/incremental-monitoring.md)控制资源和生命周期。
- 配置前阅读[CC Connect 平台选择](references/cc-connect-platform-selection.md)。
- 用[消息投递](references/delivery.md)实现队列、重试和平台边界。
- 决定消息内容前阅读[隐私](references/privacy.md)。
- 最后执行[验证清单](references/validation.md)。

## 交付物

本机方案应覆盖用户选择的范围，并至少说明：脱敏信号、支持状态与优先级、来源过滤、生命周期、隐私、可靠投递、CC Connect 平台、测试结果和已知限制。未选择的等待状态、入站控制、多平台或图形界面可以不实现；一旦实现，就按相应参考闭环。
